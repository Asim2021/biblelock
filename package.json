{
	"name": "my-fullstack-app",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"start:backend": "npm run dev --prefix backend",
		"start:frontend": "npm start --prefix frontend",
		"dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue.bold,green.bold\" \"npm run start:backend\" \"npm run start:frontend\""
	},
	"devDependencies": {
		"concurrently": "^10.0.5"
	}
}
